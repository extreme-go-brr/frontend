# --- Development/Serve Stage ---
FROM node:22-alpine

WORKDIR /app

# Copy ALL project files first, so 'quasar prepare' has everything it needs
COPY . .

# Install dependencies
RUN npm install

# Expose the port Quasar Dev Server runs on
EXPOSE 8080

# Command to run Quasar development server
CMD ["npm", "run", "dev"]